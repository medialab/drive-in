'use strict';

/**
 * @ngdoc function
 * @name drivein.controller:referencesCtrl
 * @description
 * # LayoutCtrl
 * Controller of the references page.
 */
angular.module('drivein')
  .controller('referencesCtrl', function($scope, $log, $http, $q) {
    $log.debug('referencesCtrl loaded.');
  })